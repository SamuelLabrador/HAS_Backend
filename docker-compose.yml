version: '3.7'

services:
  db:
    image: postgres
  backend:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes: 
      - type: bind
        source: .
        target: /backend
    ports:
      - "8000:8000"
    depends_on:
      - db
