version: '3.3'

services:
  cache: 
    image: memcached

  backend:
    build:
      context: .
      dockerfile: 'Dockerfile.dev'
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
    volumes: 
      - type: bind
        source: .
        target: /backend
    ports:
      - "8000:8000"
    environment: 
      - HAS_PRODUCTION=1
      - DATA_PIPELINE=0
      - DB_USER=has_client
      - DB_HOST=97.90.193.55
      - DB_PASSWORD=0112358D1522
      - DB_NAME=postgres